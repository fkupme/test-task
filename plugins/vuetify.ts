export default defineNuxtPlugin(() => {
	// Vuetify инициализируется через `vuetify-nuxt-module`.
	// Этот файл оставлен пустым, чтобы избежать двойной установки Vuetify
	// (и предупреждений вида "App already provides property ... will be overwritten").
});
